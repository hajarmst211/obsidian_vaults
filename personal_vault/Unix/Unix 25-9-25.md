- redirection d'erreurs dans linux?
- UID: 
	- 0: admin
	- 0-> UID_min=100: utilisateurs systèmes
	- UID_min -> UID_max: utilisateurs physiques
- deux utilisateurs peuvent avoir le meme UID

- root:CUSzE7 4h021 CS :0:0: Super User:/:bin/bash
	- "Super User": repertoire utilisateur
	- ":/:bin/bash": shell de l'utilisateur
	- Pour exécuter un script, c'est rendre le fichier texte executable 
NB: "!!" dans le champ mdps veut dire qu'il n'est pas encore accesible
- Pour switcher d'un shell a un autre, on tape le nom su shell. et pour sortir on tape "exit"
![[Pasted image 20250925113855.png]]
- Pour exécuter un script dans un shell specific: le nom du script + /bin/shell_name
- Pour voir les users et les passwords:
  ![[Pasted image 20250925114227.png]]
- les groupes: root::0:root
	- chanp1: nom du groupe
	- champ2: password
	- champ3: 
	- champ4: les utilisateurs appartements aux groupes
- Afficher les différents login du compte administrateur(UID=)0:
	 1. filtrer avec grep
	2. \^\[^:]\*\[^:]*0 etc/passwd : pour trouver les comptes admin
- On peut etre membre de plusieurs groupe pour profiter des privilege dont les autres en profitent
#### Commandes de gestion
- userdl: deletes everything that concerns that user(memory, authorities...)
- skelton directory??? etc/skl
- .bash_profile: script that gets executed on the login
- .bash_logout: gets executed on the logout
- .bashrc: script that defines the alias
---
les valeurs octales des droits d'acces
r- 4
w-2
x-1

`chmod 006 f`: 
	- premier 0: je vais pas toucher au droits d'utilisateru, tu vas garder ce que tu as
	- deuxieme: 6=4+2: read and write
`chmod 731 f`: rwx_wx__x

x pour un repertoire veut dire qu'on a le droit de 
le droit `s` a la place de `x` veut dire qu'au moment de l'execution l'utilisateur peut modifier le fichier
- si je suis l admin et un utilisateur a creer plusieurs fichiers dispersse en extentsion .txt il veut les transformer en .c
	- commetn changer l'unite de mesure dans la commade du, elle donne les resultats en kb je les veux en Gb
	- if i want to create a file din a full repository, how can i make it available to make it possible: on cree le fichier dans un autre repertoire et on cree un raccourcie dans le repertoire qu'on veut
	- 